# This is an example .env file for configuring environment variables.

### VERY IMPORTANT NOTE:
# CAUTION: Do not commit this file with sensitive information to version control.
# All environment variables should be set in your local environment.
# Here you can set the variables needed for running CI tests.
# values shoud be dummy values, so that the CI tests can run without errors.

export COFFEE_MAKER_RUN_CI_TESTS="True"
